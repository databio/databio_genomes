metadata:
  sample_table: archives.csv
  output_dir: $HOME/refgenieserver_archive
  pipeline_interfaces: refgenieserver_piface.yaml

constant_attributes:
  cfg: "cfg_path"
  protocol: "refgenieserver"

derived_attributes: ['cfg']

data_sources:
  cfg_path: "$PROJECT/genomes_{sample_name}/genomes.yaml"